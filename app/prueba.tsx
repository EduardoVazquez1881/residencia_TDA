/**
 * /prueba — Pantalla principal (Home Dashboard) tras inicio de sesión exitoso
 */

import { HomeScreen } from "@/components/home/home-screen";

export default function PruebaPage() {
  return <HomeScreen />;
}
